apiVersion: v1
kind: Service
metadata:
  name: backend-challenge
  namespace: assignment
spec:
  selector:
    app: backend-challenge
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
